package main

// These values may be replaced at build time with -ldflags. The repository
// can also be overridden at runtime with VIEWER_LAUNCHER_REPOSITORY.
var (
	repository = "AntaresTechno/Viewer-Launcher"
	version    = "dev"
	releaseTag = "preview"
)
